---
title: Строки
icon: lucide/case-sensitive
---

> Строки — это последовательности символов (конкретно [кодовых точек Unicode](https://en.wikipedia.org/wiki/Code_point)). Sass поддерживает два вида строк с одинаковой внутренней структурой, но которые отображаются по-разному: [строки в кавычках](#quoted), например `"Helvetica Neue"`, и [строки без кавычек](#unquoted) (также известные как *идентификаторы*), например `bold`. Вместе они охватывают различные виды текста, встречающиеся в CSS.

!!! tip "Совет"

    Вы можете преобразовать строку в кавычках в строку без кавычек с помощью [функции `string.unquote()`](../modules/string#unquote), а строку без кавычек — в строку в кавычках с помощью [функции `string.quote()`](./modules/string#quote).

    === "SCSS"

        ```scss
        @use "sass:string";

        @debug string.unquote(".widget:hover"); // .widget:hover
        @debug string.quote(bold); // "bold"
        ```

    === "SASS"

        ```sass
        @use "sass:string"

        @debug string.unquote(".widget:hover")  // .widget:hover
        @debug string.quote(bold)  // "bold"
        ```

## Escape-последовательности {#escapes}

Все строки Sass поддерживают стандартные [escape-коды](https://developer.mozilla.org/en-US/docs/Web/CSS/string#Syntax) CSS:

* Любой символ, кроме букв от A до F или цифр от 0 до 9 (даже символ новой строки!), может быть включён в строку, если написать перед ним `\`.

* Любой символ может быть включён в строку, если написать `\`, а затем номер его [кодовой точки Unicode](https://en.wikipedia.org/wiki/List_of_Unicode_characters) в [шестнадцатеричной системе счисления](https://ru.wikipedia.org/wiki/%D0%A8%D0%B5%D1%81%D1%82%D0%BD%D0%B0%D0%B4%D1%86%D0%B0%D1%82%D0%B5%D1%80%D0%B8%D1%87%D0%BD%D0%B0%D1%8F_%D1%81%D0%B8%D1%81%D1%82%D0%B5%D0%BC%D0%B0_%D1%81%D1%87%D0%B8%D1%81%D0%BB%D0%B5%D0%BD%D0%B8%D1%8F). Опционально можно добавить пробел после номера кодовой точки, чтобы указать, где заканчивается номер Unicode.

=== "SCSS"

    ```scss
    @debug "\""; // '"'
    @debug \.widget; // \.widget
    @debug "\a"; // "\a" (строка, содержащая только символ новой строки)
    @debug "line1\a line2"; // "line1\a line2" (line1 и line2 разделены символом новой строки)
    @debug "Nat + Liz \1F46D"; // "Nat + Liz 👭"
    ```

=== "SASS"

    ```sass
    @debug "\""  // '"'
    @debug \.widget  // \.widget
    @debug "\a"  // "\a" (строка, содержащая только символ новой строки)
    @debug "line1\a line2"  // "line1\a line2" (line1 и line2 разделены символом новой строки)
    @debug "Nat + Liz \1F46D"  // "Nat + Liz 👭"
    ```

!!! note "Примечание"

    Для символов, которым разрешено появляться в строках, запись escape-последовательности Unicode создаёт точно такую же строку, как и запись самого символа.

## Строки в кавычках {#quoted}

Строки в кавычках записываются между одинарными или двойными кавычками, например `"Helvetica Neue"`. Они могут содержать [интерполяцию](../interpolation), а также любые символы без экранирования, за исключением:

* `\`, который можно экранировать как `\\`;
* `'` или `"`, в зависимости от того, какие кавычки использовались для определения строки, которые можно экранировать как `\'` или `\"`;
* символов новой строки, которые можно экранировать как `\a ` (включая пробел в конце).

Гарантируется, что строки в кавычках будут скомпилированы в CSS-строки с тем же содержимым, что и исходные Sass-строки. Точный формат может варьироваться в зависимости от реализации или конфигурации — строка, содержащая двойную кавычку, может быть скомпилирована в `"\""` или `'"'`, а не-[ASCII](https://ru.wikipedia.org/wiki/ASCII) символ может быть экранирован или нет. Но это должно разбираться одинаково в любой соответствующей стандартам CSS-реализации, включая все браузеры.

=== "SCSS"

    ```scss
    @debug "Helvetica Neue"; // "Helvetica Neue"
    @debug "C:\\Program Files"; // "C:\\Program Files"
    @debug "\"Don't Fear the Reaper\""; // "\"Don't Fear the Reaper\""
    @debug "line1\a line2"; // "line1\a line2"

    $roboto-variant: "Mono";
    @debug "Roboto #{$roboto-variant}"; // "Roboto Mono"
    ```

=== "SASS"

    ```sass
    @debug "Helvetica Neue"  // "Helvetica Neue"
    @debug "C:\\Program Files"  // "C:\\Program Files"
    @debug "\"Don't Fear the Reaper\""  // "\"Don't Fear the Reaper\""
    @debug "line1\a line2"  // "line1\a line2"

    $roboto-variant: "Mono"
    @debug "Roboto #{$roboto-variant}"  // "Roboto Mono"
    ```

!!! info "Информация"

    Когда строка в кавычках внедряется в другое значение через интерполяцию, [её кавычки удаляются](../interpolation#quoted-strings)! Это упрощает написание строк, содержащих селекторы, например, которые можно внедрить в стилевые правила без добавления кавычек.

## Строки без кавычек {#unquoted}

Строки без кавычек записываются как [идентификаторы](https://drafts.csswg.org/css-syntax-3/#ident-token-diagram) CSS, следуя синтаксической диаграмме ниже. Они могут включать [интерполяцию](../interpolation) в любом месте.

![Railroad diagram copyright © 2018 W3C](identifier-diagram.svg)

=== "SCSS"

    ```scss
    @debug bold; // bold
    @debug -webkit-flex; // -webkit-flex
    @debug --123; // --123

    $prefix: ms;
    @debug -#{$prefix}-flex; // -ms-flex
    ```

=== "SASS"

    ```sass
    @debug bold  // bold
    @debug -webkit-flex  // -webkit-flex
    @debug --123  // --123

    $prefix: ms
    @debug -#{$prefix}-flex  // -ms-flex
    ```

!!! note "Примечание"

    Не все идентификаторы разбираются как строки без кавычек:

    * [Названия цветов CSS](https://developer.mozilla.org/en-US/docs/Web/CSS/color_value#Color_keywords) разбираются как [цвета](../values/colors).
    * `null` разбирается как [значение `null` в Sass](../values/null).
    * `true` и `false` разбираются как [булевы значения](../values/booleans).
    * `not`, `and` и `or` разбираются как [булевы операторы](../operators/boolean).

    Из-за этого, как правило, хорошей идеей является запись строк в кавычках, если только вы не пишете конкретно значение CSS-свойства, которое использует строки без кавычек.

### Escape-последовательности в строках без кавычек {#escapes-in-unquoted-strings}

Когда строка без кавычек разбирается, буквальный текст escape-последовательностей разбирается как часть строки. Например, `\a ` разбирается как символы `\`, `a` и пробел. Однако, чтобы гарантировать, что строки без кавычек, которые имеют одинаковые значения в CSS, разбираются одинаково, эти escape-последовательности *нормализуются*. Для каждой кодовой точки, независимо от того, экранирована она или нет:

* Если это допустимый символ идентификатора, он включается без экранирования в строку без кавычек. Например, `\1F46D` возвращает строку без кавычек `👭`.

* Если это печатаемый символ, кроме новой строки или табуляции, он включается после `\`. Например, `\21 ` возвращает строку без кавычек `\!`.

* В противном случае включается escape-последовательность Unicode в нижнем регистре с завершающим пробелом. Например, `\7Fx` возвращает строку без кавычек `\7f x`.

=== "SCSS"

    ```scss
    @use "sass:string";

    @debug \1F46D; // 👭
    @debug \21; // \!
    @debug \7Fx; // \7f x
    @debug string.length(\7Fx); // 5
    ```

=== "SASS"

    ```sass
    @use "sass:string"

    @debug \1F46D  // 👭
    @debug \21  // \!
    @debug \7Fx  // \7f x
    @debug string.length(\7Fx)  // 5
    ```

### Другие строки без кавычек {#other-unquoted-strings}

Помимо идентификаторов, существует несколько необычных уголков синтаксиса CSS, которые разбираются как строки без кавычек. К ним относятся:

* [Специальные функции](../syntax/special-functions/) вроде `url()` и `element()`, аргументы которых имеют синтаксис, отличающийся от обычного синтаксиса CSS-выражений.

* Токены [Unicode-диапазонов](https://developer.mozilla.org/en-US/docs/Web/CSS/@font-face/unicode-range) вроде `U+0-7F` или `U+4??`.

* [Hash-токены](https://www.w3.org/TR/css-syntax-3/#ref-for-typedef-hash-token%E2%91%A0), значения которых являются идентификаторами, но которые не являются допустимыми шестнадцатеричными цветами.

* Значение `%`. (Оно доступно только когда не находится непосредственно между двумя другими значениями, иначе это было бы неоднозначно с [оператором остатка от деления](../operators/numeric/).)

* Специальное значение `!important`.

=== "SCSS"

    ```scss
     @debug url(https://example.org); // url(https://example.org)
    @debug U+4??; // U+4??
    @debug #my-background; // #my-background
    @debug %; // %
    @debug !important; // !important
    ```

=== "SASS"

    ```sass
    @debug url(https://example.org)  // url(https://example.org)
    @debug U+4??  // U+4??
    @debug #my-background  // #my-background
    @debug %  // %
    @debug !important  // !important
    ```

## Индексы строк {#string-indexes}

В Sass есть ряд [строковых функций](../modules/string), принимающих или возвращающих числа, называемые *индексами* и ссылающиеся на символы в строке. Индекс 1 обозначает первый символ строки. Обратите внимание, что это отличается от многих языков программирования, где индексы начинаются с 0! Sass также упрощает обращение к концу строки. Индекс -1 ссылается на последний символ строки, -2 — на предпоследний и так далее.

=== "SCSS"

    ```scss
    @use "sass:string";

    @debug string.index("Helvetica Neue", "Helvetica"); // 1
    @debug string.index("Helvetica Neue", "Neue"); // 11
    @debug string.slice("Roboto Mono", -4); // "Mono"
    ```

=== "SASS"

    ```sass
    @use "sass:string"

    @debug string.index("Helvetica Neue", "Helvetica")  // 1
    @debug string.index("Helvetica Neue", "Neue")  // 11
    @debug string.slice("Roboto Mono", -4)  // "Mono"
    ```
