---
title: meta.feature-exists()
icon: lucide/newspaper
---

> Функция `meta.feature-exists()` давно не получала новых возможностей и теперь устарела. Пользователям следует использовать другие способы определения доступности новых возможностей.

Исторически Sass использовал функцию `meta.feature-exists()` (также доступную как глобальная функция `feature-exists()`) для того, чтобы позволить авторам определять, доступны ли различные новые языковые возможности при компиляции таблиц стилей. Однако со временем выяснилось, что подавляющее большинство новых возможностей Sass либо можно обнаружить более простым способом, либо их обнаружение вообще не очень полезно.

Эта функция теперь устарела и будет удалена в Dart Sass 2.0.0. Поскольку Dart Sass теперь является единственной официально поддерживаемой реализацией Sass, и все версии Dart Sass поддерживают все возможности, поддерживаемые `meta.feature-exists()`, все существующие использования этой функции можно безопасно удалить.

Многие новые возможности можно обнаружить с помощью [`meta.function-exists()`](../modules/meta#function-exists), [`meta.mixin-exists()`](../modules/meta#mixin-exists) или [`meta.global-variable-exists()`](../modules/meta#global-variable-exists). Другие можно обнаружить с помощью синтаксиса на уровне выражений, например, используя `calc(1) == 1`, чтобы определить, поддерживает ли текущая версия Sass вычисления первого класса.

## Переходный период {#transition-period}

Сначала мы будем выдавать предупреждения об устаревании для всех использований `feature-exists`.

В Dart Sass 2.0.0 `meta.feature-exists()` больше не будет существовать. Попытки её вызова вызовут ошибку, а попытки вызвать глобальную функцию `feature-exists()` будут рассматриваться как обычный вызов CSS-функции.

--8<-- "includes/silence-warnings.md"
